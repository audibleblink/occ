[tools]
python = "3.12"

[tasks.clean]
description = "Clean build artifacts and caches"
run = """
rm -rf ~/.config/occ/
uv tool uninstall occ
uv cache clean
"""

[tasks.install]
description = "Install occ as a uv tool"
run = "uv tool install . --force"
